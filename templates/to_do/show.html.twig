{% extends 'base.html.twig' %}

{% block title %}Task{% endblock %}

{% block body %}
    <h1>Task</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ task.id }}</td>
            </tr>
            <tr>
                <th>Title</th>
                <td>{{ task.title }}</td>
            </tr>
            <tr>
                <th>Description</th>
                <td>{{ task.description }}</td>
            </tr>
            <tr>
                <th>Completed</th>
                <td>{{ task.completed }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('app_to_do_index') }}">back to list</a>

    <a href="{{ path('app_to_do_edit', {'id': task.id}) }}">edit</a>

    {{ include('to_do/_delete_form.html.twig') }}
{% endblock %}

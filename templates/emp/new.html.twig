{% extends 'base.html.twig' %}

{% block title %}New Employee{% endblock %}

{% block body %}
    <div class="container">
        <h2 class="text-center mt-5 mb-3">Add new Employee</h2>
        <div class="card">
            <div class="card-header">
                <a class="btn btn-outline-info float-right" href="{{ path('app_emp_index') }}"> 
                    Back to list
                </a>
            </div>
            <div class="card-body">
                {{ include('emp/_form.html.twig') }}
            </div>
        </div>
    </div>
{% endblock %}
